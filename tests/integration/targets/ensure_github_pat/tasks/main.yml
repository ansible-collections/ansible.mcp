---
- name: check temporary file
  ansible.builtin.stat:
    path: /tmp/integration_config.yml
  register: st

- name: Validate that the file exists
  ansible.builtin.fail:
    msg: "The file /tmp/integration_config.yml does not exist."
  when: not st.stat.exists
